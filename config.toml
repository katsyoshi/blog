title = "katsyoshi のめもみたいなの"
author = "web@katsyoshi.org"
description =  "It's a memos."
base_url = "https://blog.katsyoshi.org" # the base hostname & protocol for your site, e.g. http://example.com

compile_sass = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[slugify]
taxonomies = "on"
