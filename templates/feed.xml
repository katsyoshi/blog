<feed>
  <generator uri="https://getzola.org">Zola</generator>
  <link href="{{config.base_url|safe}}/feed.xml" rel="self" type="application/atom+xml"/>
  <link href="{{config.base_url|safe}}" rel="alternate" type="text/html"/>
  <updated>{{ last_updated | date(format="%+", timezone="Asia/Tokyo") }}</updated>
  <id>{{config.base_url|safe}}/feed.xml</id>
  <title type="html">{{config.title}}</title>
  <subtitle>{{config.description}}</subtitle>
  {%- for page in pages %}
  <entry>
    <title>{{page.title}}</title>
    <published>{{page.date | date(timezone="Asia/Tokyo",format="%+")}}</published>
    <updated>{{page.updated | default(value=page.date) | date(format="%+", timezone="Asia/Tokyo")}}</updated>
    <id>{{page.permalink|safe|trim_end_matches(pat="/")}}</id>
    <link href="{{page.permalink|safe|trim_end_matches(pat="/")}}" rel="alternate" type="text/html" title="{{page.title}}"></link>
    <content type="text/html">{{page.content}}</content>
    <author>
      <name>
        {%- if page.authors %}
        {{ page.authors | join }}
        {%- else %}
        {{ config.author }}
        {%- endif %}
      </name>
    </author>
  </entry>
  {%- endfor %}
</feed>
